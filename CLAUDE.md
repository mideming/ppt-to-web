# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

这是一个基于《崇明岛乡村住宅》PPT研究报告创建的前端展示网站。项目将PPT中的核心内容（诉求、设计、施工三大板块）转化为交互式网页，包含轮播图、响应式设计和用户交互功能。

## 项目结构

```
pptToWeb/
├── index.html              # 主HTML文件
├── css/
│   └── style.css          # 样式文件
├── js/
│   ├── data.js            # 项目数据（从PPT提取）
│   └── main.js            # 主要JavaScript功能
├── images/                 # 图片资源目录
├── analyze_ppt.py         # PPT分析脚本（Python）
├── 崇明岛乡村住宅.pptx    # 原始PPT文件
└── CLAUDE.md              # 本项目说明文件
```

## 技术栈

- **HTML5**: 语义化标签，现代网页结构
- **CSS3**: Flexbox/Grid布局，响应式设计，CSS动画
- **JavaScript (ES6+)**: 原生JavaScript，面向对象编程
- **Font Awesome**: 图标库
- **Google Fonts**: 中文字体（Noto Sans SC）

## 核心功能

1. **响应式导航栏**: 固定定位，移动端汉堡菜单
2. **自动轮播图**: 5秒自动切换，手动控制，键盘支持（左右箭头）
3. **三大内容板块**:
   - 诉求: 年龄结构需求分析、政策支持、生态适配
   - 设计: 功能布局、平面模块、立面风格
   - 施工: 一站式流程、施工环节、质量保障
4. **交互功能**: 平滑滚动、悬停效果、点击交互
5. **响应式设计**: 适配桌面、平板、手机等设备

## 开发说明

### 数据来源
所有内容数据来自 `js/data.js`，该文件通过 `analyze_ppt.py` 脚本从PPT文件中提取并结构化。

### 主要JavaScript类
- `Carousel`: 轮播图功能，支持自动播放和手动控制
- `ContentRenderer`: 动态渲染三大板块内容
- `Navigation`: 导航功能，包括移动端菜单和平滑滚动

### 样式特点
- 现代简约设计风格
- 每个板块使用不同主题色:
  - 诉求: 蓝色主题 (#3498db)
  - 设计: 绿色主题 (#2ecc71)
  - 施工: 红色主题 (#e74c3c)
- 卡片式布局，悬停动画效果
- 渐变背景和阴影效果

## 运行方式

直接在浏览器中打开 `index.html` 文件即可运行，无需后端服务器。

## 扩展建议

1. **添加真实图片**: 在 `images/` 目录中添加项目相关图片
2. **优化性能**: 图片懒加载，代码分割
3. **添加更多交互**: 表单提交，地图集成，3D模型展示
4. **多语言支持**: 添加英文版本
5. **SEO优化**: 添加meta标签，结构化数据

## 注意事项

- 当前使用CSS渐变作为轮播图背景，可替换为实际项目图片
- 所有交互功能使用原生JavaScript实现，无外部依赖
- 项目已针对中文内容优化，确保字体和排版正确